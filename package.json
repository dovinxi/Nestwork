{
  "name": "nestwork",
  "private": true,
  "version": "0.1.0",
  "description": "NestWork - a personal relationship & contact manager",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/server & npm run dev --workspace=apps/web",
    "dev:server": "npm run dev --workspace=apps/server",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "npm run typecheck --workspace=packages/shared && npm run build --workspace=apps/server && npm run build --workspace=apps/web",
    "db:migrate": "npm run db:migrate --workspace=apps/server",
    "db:studio": "npm run db:studio --workspace=apps/server"
  }
}
